# App Icon

Place a custom Windows icon here named `app.ico`.

Recommended size: 256x256 with 32-bit color.

This icon is used by:
- the built EXE (PyInstaller)
- the MSI installer
- the Start Menu shortcut script
